name: Cypress Tests

on: [push]

jobs:
  cypress-electron-tests:
    runs-on: unbutu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4.1.0

      - name: Install dependencies
        run: yarn install

      - name: Run E2E tests
        run: npx cypress run
      
